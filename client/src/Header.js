import styled from "styled-components";

const Header = () => {
  return <div> This is the header for telling player turns/winner/loser!</div>;
};

export default Header;

const Div = styled.div`
  height: 50px;
  width: 100 vw;
`;
